# design documents stay local until published with official versions
docs/

# toolchain (populated once the Android project lands)
.gradle/
build/
local.properties
.idea/
*.apk

# macos
.DS_Store
build.nosync/

# claude code local tooling + kotlin build cache (machine-specific)
.claude/
.kotlin/

# se-skills session workspace (working memory; durable facts live in docs/)
.se/
